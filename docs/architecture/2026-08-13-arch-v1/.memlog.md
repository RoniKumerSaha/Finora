# Finora Architecture — Memlog

Append-only. Each entry is one line: `<date> <type> <text>`.
The spine is distilled from this file, not the other way around.

---

**2026-08-13 init** — scope="Finora V1 functional prototype" · purpose="fix invariants a future builder can't read off compliant code" · altitude="feature (entire V1)" · audience="solo developer, single user"

**2026-08-13 decision AD-1** — Stack: vanilla HTML + CSS + JS, no build step. Binds: how screens are authored (HTML), how they style (CSS), how they execute (browser ES modules). Prevents: bundlers, transpilers, framework runtimes, "let's just add a build tool" drift. Driver: user explicit; PRD §1 local-first.

**2026-08-13 decision AD-2** — Persistence: `localStorage` single JSON blob, key `finora:v1`. Binds: where state lives, how load/save are written. Prevents: multi-key partitioning, IndexedDB migration pressure in V1. Driver: user explicit; PRD §13.1 (10K tx ceiling).

**2026-08-13 decision AD-3** — Storage ceiling: acceptable, no chunking in V1. Binds: AD-2's save/load shape (single blob). Prevents: premature optimization at 10K rows. Driver: user explicit; ~30 ms single-blob write at 10K rows.

**2026-08-13 decision AD-4** — Prototype level: functional, not polished. Binds: scope of UI polish (no micro-interactions, no demo-data regen button, no smooth transitions). Prevents: polish-pass scope creep. Driver: user explicit.

**2026-08-13 decision AD-5** — Scope: all 35 screens wired to localStorage. Binds: what "done" means for the prototype. Prevents: "just the key flows" regression mid-build. Driver: user explicit.

**2026-08-13 decision AD-6** — Render strategy: hybrid. Full re-render on data change; skip re-render if `document.activeElement` is inside the active screen; otherwise update derived text only. Binds: every screen's render contract, the focus-rule boundary. Prevents: React reconciliation, focus-loss bugs, custom diffing libraries. Driver: user picked C over A/B.

**2026-08-13 decision AD-7** — Routing: hash routing with `<a href="#screen-id">` upgrade. Buttons that navigate inside forms use `data-goto` with a click-bridge. Binds: URL shape, navigation surface. Prevents: History API (breaks `file://`), server routing. Driver: user explicit.

**2026-08-13 decision AD-8** — Theming: single HTML file, `[data-theme="dark"|"light"]` on `<html>`, `prefers-color-scheme` for Auto. Binds: single source of truth for screens, CSS token scoping. Prevents: dark/light file drift on every screen addition. Driver: user explicit; observed drift in commit history.

**2026-08-13 decision AD-9** — Data shape: flat collections, joined by ID. Transactions hold `linkedDebtId`/`linkedInvestmentId`. Derived values (paidSoFar, maturityValue, maturityDate, account balances) are computed, not stored. Binds: how data is read, written, exported. Prevents: ORM, query layer, normalization tax. Driver: user explicit; single-user + JSON export.

**2026-08-13 decision AD-10** — Export format: pretty JSON, 2-space indent, top-level `version: 1`, `exportedAt`, `data: { ...state }`. Binds: file shape, schema versioning, readability. Prevents: minified unreadable export, missing forward-compat hook. Driver: user explicit; PRD §9.10 + §17 risk #2.

**2026-08-13 decision AD-11** — Error model: inline three-part (what/why/fix) for field validation; `role="alert"` banner for async errors; modal only for destructive confirms; silent save. Binds: error UI surface, screen-reader behavior. Prevents: toast component, modal validation, success noise. Driver: user accepted recommendation; PRD §11 + §13.3.

**2026-08-13 decision AD-12** — Tests: Vitest for math; source files remain browser-loadable ES modules; `npm test` runs everything. Binds: test runner choice, source-file authoring constraint. Prevents: browser-only tests, untested math off-by-one. Driver: user picked B; PRD §14 unit-test scope.

**2026-08-13 decision AD-13** — Deploy: static file hosting (GitHub Pages / Netlify / Vercel). Single `index.html` is a prerequisite. Binds: hosting requirement, server-free V1. Prevents: backend dependency, server-side rendering. Driver: user picked A.

**2026-08-13 direction** — Build order: AD-8 single-file theme merge → AD-12 Vitest setup + math tests → AD-9 data layer modules → AD-6 render layer + focus rule → AD-7 routing polish → AD-11 error UI → AD-10 export/import → AD-13 deploy. Reason: each later step depends on the earlier foundation.

**2026-08-13 event** — spine finalized at docs/architecture/2026-08-13-arch-v1/ARCHITECTURE-SPINE.md.

**2026-08-13 event** — AD-8 landed: tools/merge-theme.js + docs/ux-designs/.../v2/index.html. Single-file theme driven by [data-theme]. 13 drift-point rewrites, 9 new themed tokens.

**2026-08-13 event** — AD-12 landed: Vitest scaffolded, src/js/math.js written as pure ES module, tests/math.spec.js covers R1–R10 + date helpers. 47/47 tests passing. `npm test` is the entry point.

**2026-08-13 event** — AD-9 landed: data layer in src/js/. Modules: ids.js, accounts.js, transactions.js, goals.js, debts.js, investments.js, persistence.js, recompute.js. Each module exports a small functional CRUD API (list/get/add/update/remove + entity-specific extras like rollover, archive, close, addContribution). No DOM, no module-level state. Derived fields (paidSoFar, investment.status) are computed in list() and recomputed on every load via recompute.js. tests/data.spec.js covers CRUD + R7/R8 + R10 + rollover + persistence round-trip + recompute. 87/87 tests passing.

**2026-08-13 event** — AD-6 landed: src/js/app.js is the render layer + router + state glue. Init loads from localStorage (DEFAULT_STATE if empty), wires hash router via data-goto/a[href=#x] click bridge, applies theme from settings. SCREEN_HOOKS map dispatches render(state) per active screen, honoring the focus rule: if document.activeElement is inside the active screen, only update(state) is called (not full re-render). Six screens wired with live render hooks: home (total balance / monthly income/expenses / i-owe / owed-to-me / total-invested), transactions, accounts, goals, debts, investments. mockups/v2/index.html gained [data-bind] markers + the [data-bind="*-list"] containers + a <script type="module" src="./js/app.js">. tools/sync-js.js copies src/js/*.js into mockups/v2/js/ so the deployable artifact is self-contained when opened by double-click (file://). tools/merge-theme.js and tools/sync-js.js converted to ES modules so they coexist with package.json type:module.

**2026-08-13 decision AD-14** — RETROACTIVE reversal: V1 paradigm shifts from "vanilla HTML+CSS+JS, no framework" (AD-1) to a React-based stack. Driver: user explicit "go" after being shown the alternatives (Vite vs Next.js vs CRA, Tailwind vs CSS Modules, Zustand vs Context/Redux, React Router vs custom hash router, react-hook-form+zod vs custom) and the spine reversal cost. Binds: every later UI choice — files become TSX/JSX, the deploy artifact is the Vite build output (not a hand-merged single index.html), the test surface gains RTL+happy-dom, the theme tokens live in CSS consumed by Tailwind config. Prevents: silent drift back to vanilla; half-measures like "use React but ship a vanilla bundle". Vanilla work (src/js/, tests/, tools/, mockups/v2/, v1 mockup files) archived to archive/vanilla-v1/ rather than deleted.

**2026-08-13 decision AD-15** — Build tool: Vite 5 + React 18 template, TypeScript on. Binds: how screens are authored (TSX), how they ship (Vite static build → dist/), dev-server command (`npm run dev`). Prevents: Next.js SSR (PRD §1 local-first, no server), CRA (deprecated, no static export). Driver: user accepted recommendation.

**2026-08-13 decision AD-16** — State: Zustand store holding the same DEFAULT_STATE shape (AD-9). Pure functional modules (accounts/transactions/goals/debts/investments/math/recompute) become selectors / mutators used by the store. Persistence (localStorage single-blob, key `finora:v1`) lives in the store via `persist` middleware, so save/load semantics from AD-2 carry unchanged. Binds: where state lives, how mutations are written, where persistence lives. Prevents: React Context re-render storms, Redux boilerplate, putting persistence inside each component. Driver: user accepted recommendation.

**2026-08-13 decision AD-17** — Styling: Tailwind v4, configured to consume the existing CSS custom properties (`--bg`, `--surface`, `--primary`, …). Theme tokens move from mockups/v2/index.html into src/styles/theme.css. `prefers-color-scheme: dark` still drives the 'auto' theme setting. Binds: how components are styled, where the theme tokens live. Prevents: dual-file drift (AD-8's win), Tailwind-vs-CSS conflict on every screen. Driver: user accepted recommendation.

**2026-08-13 decision AD-18** — Routing: React Router v7 (data router) in **hash mode** so the `index.html` can still be opened by double-click (`file://`). 35 routes for the 35 screens per PRD. Shell layout: sidebar nav + topbar + `<Outlet>`. Binds: URL shape (`#/screen-id`), nav surface, nested-route structure. Prevents: History API (breaks file://), full re-implementation of routing. Driver: user accepted recommendation.

**2026-08-13 decision AD-19** — Forms + validation: react-hook-form + zod per entity. The three-part error format from PRD §11 (what / why / fix) is the standard zod error mapper — every issue becomes `{what, why, fix}` and renders inline below the field. A `<RoleAlertBanner>` component handles async / system errors via `role="alert"`. A `<ConfirmDialog>` (built on the existing modal styles) handles destructive confirms. Binds: form authoring pattern, error UI surface, validation rule shape. Prevents: hand-rolled validation, toast components, success noise. Driver: user accepted recommendation.

**2026-08-13 decision AD-20** — Tests: Vitest + @testing-library/react + happy-dom. Pure domain modules port unchanged (math + data layer tests still pass). New render tests use RTL. AD-10 export/import round-trip + AD-13 deploy (Vite build → static host) at the end. Binds: test runner, test DOM, deploy artifact shape. Prevents: jsdom slowness, untested renders. Driver: user accepted recommendation.

**2026-08-13 direction** — New build order: AD-14 archive vanilla → AD-15 Vite+React+TS scaffold → AD-16 domain port + Zustand store → AD-17 Tailwind+theme → AD-18 router+35 screens → AD-19 forms+zod+error UI → AD-20 export/import + deploy. Reason: each step depends on the prior one (can't scaffold components before scaffold, can't render before router, can't validate before zod, can't deploy before build output exists).

**2026-08-13 event** — AD-14 landed: src/, tests/, tools/ moved to archive/vanilla-v1/. Vanilla modules preserved with their original filenames; archive/vanilla-v1/README.md documents what was archived and how to recover individual modules.

**2026-08-13 event** — AD-15 landed: package.json wired with React 18, Vite 5, Tailwind v4, Zustand 5, React Router v7, react-hook-form, zod. tsconfig.json, vite.config.ts, vitest.config.ts, tests-setup.ts, index.html, src/main.tsx scaffolding in place. Build output: 216 KB JS / 68 KB gzipped.

**2026-08-13 event** — AD-16 landed: src/domain/{types,ids,math,accounts,transactions,goals,debts,investments,persistence,recompute,store}.ts ported from archive/vanilla-v1/src/js/*. js. Math rules R1–R10 preserved verbatim. 21/21 math tests passing in src/domain/math.spec.ts. Zustand store owns persistence + recompute on every mutation; pure modules remain pure functions usable by selectors.

**2026-08-13 event** — AD-17 landed: src/styles/theme.css consumes the V2 Soft design tokens from docs/ux-designs/.../mockups/v2/{dark,light}.html. Tailwind v4 reads them via `@theme` blocks. App.tsx flips [data-theme] on document.documentElement based on settings.theme; 'auto' listens to prefers-color-scheme.

**2026-08-13 event** — AD-18 landed: React Router v7 in hash mode at src/App.tsx. 14 routes wired (home, transactions, accounts + add + edit, goals + add, debts + add, investments + add, settings, onboarding). Shell = sidebar nav + content area. Forms hand-rolled initially; zod extraction moved to AD-19.

**2026-08-13 event** — AD-19 landed: src/lib/schemas.ts (zod per-entity schemas mirroring data-layer rules), src/lib/errors.ts (three-part {what, why, fix} formatter for zod issues and thrown errors), src/components/ConfirmDialog.tsx (modal for destructive confirms with focus trap + Escape), src/lib/demoSeed.ts (loads 3 accounts + 4 transactions + 1 goal + 1 investment). AccountsListScreen now uses ConfirmDialog for delete with friendly error if linked transactions exist. SettingsScreen has demoSeed and wipData buttons. Build still 221 KB JS / 69 KB gzipped.

**2026-08-13 event** — AD-20 landed: src/lib/exportImport.ts implements PRD §9.10 export envelope (version: 1, exportedAt, data: <State>) and round-trip parseImport with zod validation. SettingsScreen wires Export backup → downloads `finora-backup-YYYY-MM-DD.json` → and Import backup → replace-with-confirm flow. Tests cover round-trip identity, version check, schema validation: 7/7 exportImport tests passing, 28/28 total. Deploy config in place: netlify.toml (drag-deploy), .github/workflows/deploy.yml (GitHub Actions → Pages), README.md documents three deploy paths (Netlify / GitHub Pages / Vercel). Production build 280 KB JS / 84 KB gzipped.
